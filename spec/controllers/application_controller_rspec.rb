describe ApplicationController do
  

  
  # it "has a valid guest user" do
  #   controller = ApplicationController.new
  #   controller.guest.should be_valid
  #   controller.guest.should have_same_attributes_as(User.first)
  #   controller.guest.should have_same_attributes_as(User.last)
  #   User.count.should be 1
  # end
end